# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# Virtual environments
venv/
env/
ENV/
.venv/
# Chatterbox/Nano pilot venv (pinned numpy<2 + torch 2.6 — must never merge
# with the bot venv; see GPU_UPGRADE_PLAN.md)
chatterbox-venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env

# Logs
*.log
logs/

# Generated audio
out/
tmp/

# Piper voice models (downloaded at runtime)
piper_voices/

# RVC models and indexes (referenced via config, not committed)
models/

# RVC voice models (large binaries; referenced via config, not committed)
rvc_models/

# Reference clips for zero-shot TTS/VC pilots (user-supplied recordings)
reference-*.opus
reference-*.wav

# Generated per-voice pitch profiles (tools/analyze_voices.py, keyed by
# model path + mtime + tuning; regenerate any time)
config/voice_pitch.json